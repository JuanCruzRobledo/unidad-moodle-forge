# Dependencias Python de la skill
# python -m pip install -r requirements.txt
# Despues de instalar playwright, correr tambien: playwright install chromium
playwright
pyyaml

# Solo necesaria para scripts/resize_imagen_banner.py (forzar la imagen de
# banner de Introduccion, Fase 1, a 600x600px exactos tras generarla)
Pillow

# Solo necesarias para scripts/gamma_generate.py (generacion opcional de
# Material de apoyo via la API de Gamma, requiere cuenta paga),
# scripts/configurar.py (lee/escribe el .env de esta maquina) y
# scripts/render_pdf.py (lee PDF_HEADER_LOGO, la imagen opcional del
# encabezado de los PDF con membrete)
requests
python-dotenv
