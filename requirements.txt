# Dependencias Python de la skill
# python -m pip install -r requirements.txt
# Despues de instalar playwright, correr tambien: playwright install chromium
playwright
pyyaml

# Solo necesarias para scripts/gamma_generate.py (generacion opcional de
# Material de apoyo via la API de Gamma, requiere cuenta paga) y
# scripts/configurar.py (lee/escribe el .env de esta maquina)
requests
python-dotenv
